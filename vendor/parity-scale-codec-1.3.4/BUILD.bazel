"""
@generated
cargo-raze crate build file.

DO NOT EDIT! Replaced on runs of cargo-raze
"""

# buildifier: disable=load
load(
    "@io_bazel_rules_rust//rust:rust.bzl",
    "rust_binary",
    "rust_library",
    "rust_test",
)

# buildifier: disable=load
load("@bazel_skylib//lib:selects.bzl", "selects")

package(default_visibility = [
    # Public for visibility by "@raze__crate__version//" targets.
    #
    # Prefer access through "//", which limits external
    # visibility to explicit Cargo.toml dependencies.
    "//visibility:public",
])

licenses([
    "notice",  # Apache-2.0 from expression "Apache-2.0"
])

# Generated Targets

# Unsupported target "benches" with type "bench" omitted

rust_library(
    name = "parity_scale_codec",
    srcs = glob(["**/*.rs"]),
    crate_features = [
        "bitvec",
        "default",
        "derive",
        "full",
        "parity-scale-codec-derive",
        "serde",
        "std",
    ],
    crate_root = "src/lib.rs",
    crate_type = "lib",
    data = [],
    edition = "2018",
    proc_macro_deps = [
        "//vendor/parity-scale-codec-derive-1.2.1:parity_scale_codec_derive",
    ],
    rustc_flags = [
        "--cap-lints=allow",
    ],
    tags = [
        "cargo-raze",
        "manual",
    ],
    version = "1.3.4",
    # buildifier: leave-alone
    deps = [
        "//vendor/arrayvec-0.5.1:arrayvec",
        "//vendor/bitvec-0.17.4:bitvec",
        "//vendor/byte-slice-cast-0.3.5:byte_slice_cast",
        "//vendor/serde-1.0.114:serde",
    ],
)

# Unsupported target "mod" with type "test" omitted

# Unsupported target "single_field_struct_encoding" with type "test" omitted

# Unsupported target "skip" with type "test" omitted

# Unsupported target "variant_number" with type "test" omitted
